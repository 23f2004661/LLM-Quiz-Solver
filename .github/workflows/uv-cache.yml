name: Uv set up and cache

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: cache dependencies
        id: cache-first
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-ad39371

      - name: prime-cache-ad39371
        run: echo "Cache hit? ${{ steps.cache-first.outputs.cache-hit }}"

